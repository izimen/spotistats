/**
 * Profile Routes (Placeholder for Phase 2)
 * Will handle public user profiles
 */
const express = require('express');
const router = express.Router();
const { optionalAuth } = require('../middleware/protect');

// Placeholder routes for Phase 2
// router.get('/:username', optionalAuth, profileController.getProfile);
// router.put('/settings', protect, profileController.updateSettings);

module.exports = router;
